import { FilterByModulePipe } from './filter-by-module.pipe';

describe('FilterByModulePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByModulePipe();
    expect(pipe).toBeTruthy();
  });
});
