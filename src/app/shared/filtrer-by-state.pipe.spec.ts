import { FiltrerByStatePipe } from './filtrer-by-state.pipe';

describe('FiltrerByStatePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrerByStatePipe();
    expect(pipe).toBeTruthy();
  });
});
