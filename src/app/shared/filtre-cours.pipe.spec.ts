import { FiltreCoursPipe } from './filtre-cours.pipe';

describe('FiltreCoursPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreCoursPipe();
    expect(pipe).toBeTruthy();
  });
});
